# Phase 0 Expected Outputs

This directory reserves the expected-output location for the small prepared
proteome fixture.

The first executable prepared-proteome Nextflow phase must populate this
baseline from the legacy bash workflow, then compare Nextflow outputs against
the normalized FASTA and TSV files stored here.
